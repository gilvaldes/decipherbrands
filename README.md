# Decipher Landing Page

Single-page static landing page showing the Decipher logo on a Decipher-branded background (`#0f172a`).

## Deploy on Railway

Railway will auto-detect the `Dockerfile` and deploy this as a static NGINX site.
